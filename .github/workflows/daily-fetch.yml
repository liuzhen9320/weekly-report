# Generated by: github-weekly-reporter setup
# Docs: https://github.com/deariary/github-weekly-reporter

name: Daily Fetch

on:
  schedule:
    - cron: '0 16 * * *'  # midnight Asia/Shanghai
  workflow_dispatch:

permissions:
  contents: write

jobs:
  daily:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: deariary/github-weekly-reporter@e406cfda256ab9546e5b6a9859616f22b696065e
        with:
          github-token: ${{ secrets.GH_PAT }}
          username: 'liuzhen9320'
          mode: 'daily'
          language: 'zh-CN'
          timezone: 'Asia/Shanghai'
